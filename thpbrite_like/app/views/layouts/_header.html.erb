<div class="maxime">


<nav class="navbar navbar-inverse navbar-fixed-left">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <% if !logged_in? %>
        <a class="navbar-brand" href="<%= root_path %>">THP-brite Like</a>
        <% else %>
        <a class="navbar-brand" href="<%= root_path %>">Hello <%= current_user.name %></a>
        <% end %>
      </div>
      <div id="navbar" class="navbar-collapse collapse">
        <ul class="nav navbar-nav">
          <% if !logged_in? %>
            <li><%= link_to "Sign up", new_user_path, class: "btn-lg btn-success" %></li>
            <li><a href="<%= login_path %>">Log in</a></li>
          <% else %>
            <li><a href="<%= admin_path %>">Gestion des utilisateurs</a></li>
            <li><%= link_to "Log out", logout_path, method: :delete %></li>
          <% end %>
          <!-- <li><a href="<%= admin_path %>">Admin pas caché</a></li> -->

        </ul>
      </div>
    </div>
  </nav>
</div>
